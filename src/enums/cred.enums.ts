export enum CredCardStatus {
  NotConnected = 'NOT_CONNECTED',
  NeedToSubmit = 'NEED_TO_SUBMIT',
  Waiting = 'WAITING',
  Connected = 'CONNECTED',
  Updating = 'UPDATING',
}

export enum CredStatus {
  NotVerified = 'not_verified',
  Waiting = 'waiting',
  Verified = 'verified',
}
